apiVersion: v1
kind: Service
metadata:
  name: {{ template "neo-product.fullname" . }}
  labels:
    app: {{ template "neo-product.name" . }}
    chart: {{ template "neo-product.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "neo-product.name" . }}
    release: {{ .Release.Name }}
